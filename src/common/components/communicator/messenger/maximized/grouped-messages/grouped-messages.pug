.grouped-messages(data-ng-class="{'interlocutor': !$ctrl.isMine}")
  div
    .message(data-ng-repeat="message in $ctrl.messages track by $index" data-ng-class="{'is-active': message.isNew}")
      .content
        span(data-ng-bind-html="::message.body | message")
