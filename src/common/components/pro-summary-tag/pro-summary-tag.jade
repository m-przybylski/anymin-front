.pro-summary-tag
  span.title-row {{ $ctrl.title | translate }}
    span ({{ $ctrl.tags.length }})
       
  ul.tag-list.slide-page
    li(ng-repeat="tag in $ctrl.tags | limitTo: 7")
      a.slide-btn-tag(data-ui-sref="app.search-result({q: tag})") {{ tag }}
  
    li.showMoreTagsBtn(data-ng-show="$ctrl.showTags" data-ng-click="$ctrl.showMoreTags()")
      a.slide-btn-tag
        span.pro-tag(data-ng-show="!($ctrl.showTagClass)") ...
        span.pro-tag.icon.icon-arrow-up-12(data-ng-show="$ctrl.showTagClass")
