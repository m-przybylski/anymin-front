.radio
  div
    input(
      type="radio"
      id="{{ $ctrl.id }}"
      name="$ctrl.name"
      value="{{$ctrl.value}}"
      ng-model="$ctrl.ngModel"
      ng-change="$ctrl.click()"
    )
    label.label

  label.text(for="{{ $ctrl.id }}") {{$ctrl.label | translate }}
