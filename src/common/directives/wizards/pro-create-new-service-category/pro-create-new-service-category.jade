extends ../../../templates/wizard/section-controlls.partial.jade

block edit-section
  form.category-wizard(novalidate="" role="form" name="createNewServiceForm" data-ng-hide="config.toggles.past && !config.toggles.beingEdited")
    h2.text-center.margin-top-100 KATEGORIA
    div.category-box-container
      div(data-ng-class="category == model.category ? 'category-box-selected' : 'category-box'"
      data-ng-repeat="category in categories" data-ng-click="model.category = category")
        span
        span {{ category.name }}
        i.icon.icon-yes

block done-section
  h5.color-secondary.uppercase.text-bold {{ 'COMMON.DIRECTIVES.PRO_CREATE_NEW_SERVICE_NAME.CATEGORY_NAME' | translate }}
  span {{ model.category.name }}
