.pro-switcher
    .checkbox-switcher(data-ng-click="onClickCallback()")
        input(type="checkbox" id='{{id}}'
        value="true"
        data-ng-model="ngModel")
        label(for="{{id}}")
        span Aktualnie dostępni eksperci