.security-options.security
  header.entry-title
    span(data-translate="SETTINGS.SECURITY.TITLE")

  .list-row
    .list-icon
      span.icon.icon-lock-24

    .list-data
      span.name(data-translate="SETTINGS.SECURITY.PASSWORD.LABEL")

    a.btn.btn-small.btn-link(data-translate="SETTINGS.GENERAL.FULL-NAME.BUTTON.EDIT"
    data-ng-click=":: vm.openSecurityChangePasswordSettingsModal()")

  .list-row(data-ng-if="vm.hasMobilePin")
    .list-icon
      span.icon.icon-pin-number-24

    .list-data
        span.name(data-translate="SETTINGS.SECURITY.PIN_NUMBER.LABEL")

    a.btn.btn-small.btn-default-border(data-translate="SETTINGS.GENERAL.FULL-NAME.BUTTON.EDIT"
    data-ng-click=":: vm.openSecurityPinSecuritySettingsModal()")

.security-options.manage-devices
  h2(data-translate="SETTINGS.SECURITY.MANAGE_DEVICES.TITLE")
  manage-devices(
    data-on-device-remove="vm.removeSession"
    data-ng-repeat="session in vm.sessions"
    data-device="{{ session.device }}"
    data-api-key="session.apiKey"
    data-ng-class="{'current-session': vm.checkIsCurrentSession(session.apiKey) }"
    data-device-in-use-status="session.status"
    data-device-localization="{{ session.city }}"
    data-device-system="{{ session.system }}")
